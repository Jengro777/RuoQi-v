name: network
description: "RuoQi network infrastructure: dio wrapper, unified errors and auth abstraction."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  dio: ^5.9.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
