# 平台总览门户

中台运营者入口，提供全局仪表盘和平台级配置。

## 业务边界

- 平台仪表盘、系统参数、字典/菜单等全局配置
- 中台账号管理：仅在中台内部开通，不支持外部自注册
- 不管理具体租户（见 `platform_tenant_portal_api`）或工作区（见 `platform_workspace_portal_api`）

## 账户模型

- 中台账号仅用于登录中台管理面，不能登录外部用户端
- 中台账号只能由已有中台账号持有者在管理面创建，无外部注册入口
- 外部账号不能登录中台管理面
- 两套体系物理隔离，互不穿透
