# 工作区服务

工作区协作的服务模块（HTTP + RPC），承载工作区管理门户。

## 在架构中的位置

```
workspace_portal_api → 外部用户主入口 → 查看工作区列表 → 进入工作区 → 使用应用
```

## 业务范围

- 工作区 CRUD、应用分配、成员与权限管理
- 默认工作区：外部用户注册时自动创建，绑定宿主租户
- 不执行具体业务操作（实际使用跳转到域内角色门户）

## 子门户

| 目录 | 用户 |
|---|---|
| `workspace_portal_api` | 外部用户（工作区管理员和成员） |
